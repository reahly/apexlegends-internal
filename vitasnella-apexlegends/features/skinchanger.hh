#pragma once
#include <string>
#include <vector>

namespace skinchanger {
    std::vector<std::string> skins_combo( );
    int weapon_id( );
    std::vector<std::string> charm_list( );
    void init( );
}